{
    "installCommand": "git clone --depth 1 -b 3.41.3 https://github.com/flutter/flutter.git $HOME/flutter && $HOME/flutter/bin/flutter precache --web",
    "buildCommand": "$HOME/flutter/bin/flutter build web --release",
    "outputDirectory": "build/web"
}